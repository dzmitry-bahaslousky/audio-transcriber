# ==========================================
# Audio Transcriber Service Configuration
# ==========================================

# -----------------------------
# Required Configuration
# -----------------------------

# HuggingFace API token for accessing speaker diarization models (pyannote.audio)
# Get your token from: https://huggingface.co/settings/tokens
# Required for speaker identification features
HF_TOKEN=your_huggingface_token_here

# -----------------------------
# Model Configuration
# -----------------------------

# WhisperX model size to use for transcription
# Options: tiny, base, small, medium, large-v2, large-v3
# Larger models are more accurate but require more GPU memory and processing time
# Default: base
ASR_MODEL=base

# Device to run inference on
# Options: cuda, cpu, cuda:0, cuda:1 (for multi-GPU systems)
# Default: auto-detected (cuda if GPU available, otherwise cpu)
# ASR_DEVICE=cuda

# Computation type for model inference
# Options:
#   - float32: Highest accuracy, slowest, requires most memory
#   - float16: Good balance of speed and accuracy (GPU only)
#   - int8: Fastest, uses least memory, slight accuracy loss
# Default: float32 for GPU, int8 for CPU
# ASR_COMPUTE_TYPE=float32

# Directory path where models are cached after download
# Models are several GB in size and will be reused across sessions
# Default: ~/.cache/whisper
# ASR_MODEL_CACHE_PATH=/path/to/model/cache

# -----------------------------
# Resource Management
# -----------------------------

# Idle timeout in seconds before unloading models from memory
# Set to 0 to keep models loaded indefinitely (recommended for high-traffic usage)
# Set to a positive value (e.g., 300 for 5 minutes) to free GPU memory when idle
# Models will be automatically reloaded on next request
# Default: 0 (never unload)
ASR_MODEL_IDLE_TIMEOUT=0

# Batch size for processing audio segments
# Higher values may speed up processing but use more memory
# Lower values use less memory but may be slower
# Default: 4
ASR_BATCH_SIZE=4

# -----------------------------
# Audio Processing
# -----------------------------

# Audio sample rate in Hz for processing
# WhisperX expects 16000 Hz audio
# Audio files will be resampled to this rate automatically
# Default: 16000
SAMPLE_RATE=16000